package com.zarnab.panel.auth.model;

/**
 * Distinguishes between a natural (individual) and legal (company) user profile.
 */
public enum UserProfileType {
    NATURAL, // حقیقی
    LEGAL    // حقوقی
}
