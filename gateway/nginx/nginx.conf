; worker_processes 1;
;
; events {}
;
; http {
;     upstream backend {
;         server app:8080;  # internal Docker service
;     }
;
;     server {
;         listen 80;
;         server_name zarnab.kiwiapp.ir;
;
;         # Redirect HTTP → HTTPS
;         return 301 https://$host$request_uri;
;     }
;
;     server {
;         listen 443 ssl;
;         server_name zarnab.kiwiapp.ir;
;
;         ssl_certificate /etc/letsencrypt/live/zarnab.kiwiapp.ir/fullchain.pem;
;         ssl_certificate_key /etc/letsencrypt/live/zarnab.kiwiapp.ir/privkey.pem;
;
;         # API proxy only
;         location /api/ {
;             proxy_pass http://backend;  # internal Docker network
;             proxy_set_header Host $host;
;             proxy_set_header X-Real-IP $remote_addr;
;             proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
;             proxy_set_header X-Forwarded-Proto $scheme;
;         }
;
;         # Optional: serve React files if needed (otherwise skip this in backend Nginx)
;         # location / {
;         #     root /usr/share/nginx/html;
;         #     index index.html;
;         #     try_files $uri /index.html;
;         # }
;     }
; }
